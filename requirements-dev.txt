# Development dependencies for running tests/lint/format.
# Install with: pip install -r requirements-dev.txt
# Runtime dependencies for the API live in reencoder-api/requirements.txt.
# The worker is stdlib-only.

pytest==8.4.2
httpx==0.27.2     # required by fastapi.testclient
ruff==0.6.9       # linting (M-010)
black==24.10.0    # formatting (M-010)
