fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
aiofiles==24.1.0
pydantic==2.9.2
# M-029: DB layer. SQLAlchemy 2.x runs the same code on SQLite and Postgres.
# psycopg2-binary is only used when DB_BACKEND=postgres at runtime.
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
# v3.5: Roadmap YAML loader. PyYAML is small + stdlib-style, safe_load only.
PyYAML==6.0.2
